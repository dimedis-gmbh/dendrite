{
  "name": "dendrite-e2e-tests",
  "version": "1.0.0",
  "description": "End-to-end tests for Dendrite file manager",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "test:ui": "playwright test --ui",
    "test:ci": "node tests/e2e/check-port.js && playwright test",
    "pretest": "node -e \"try { require('child_process').execSync('pkill -f dendrite.*3001 2>/dev/null || killall -q dendrite 2>/dev/null || taskkill /F /IM dendrite.exe 2>NUL', {stdio: 'ignore'}); } catch(e) {}\"",
    "test:local": "npm run pretest && npm test"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}